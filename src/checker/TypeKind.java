package checker;

/** Pickle Cannon language data type kinds*/
public enum TypeKind {
	/** Integer base type. */
	INT,
	/** Boolean base type. */
	BOOL,
	/** Array compound type. */
	ARRAY,
	/** Procedure type. */
	PROC;
}
